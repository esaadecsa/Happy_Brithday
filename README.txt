HappyBirthday — FIXED

Upload these three files to the repository root:
- index.html
- style.css
- script.js

Keep your existing folders:
- img/
- music/

IMAGE:
The HTML first loads img/hbd1.png. If that is not found, it automatically tries hbd1.png in the repository root. So hbd1.png can be inside img/ or next to index.html. Filename case must match exactly.

MICROPHONE:
The blow button requests microphone permission from a user click, resumes AudioContext if needed, and uses a more tolerant sustained-air detector. Tap the flame remains a guaranteed fallback. GitHub Pages must be opened over HTTPS and Chrome must be allowed to use the microphone.

IMPORTANT:
Replace all three HTML/CSS/JS files together. Do not mix files from older AI versions.
